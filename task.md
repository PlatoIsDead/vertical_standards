# Task List

> Статус пакета №1–№5: код готов и запушен (`8a5fcca`), 95 тестов зелёные.
> №11 «Префиксы → роли → курсы» реализована (PRPs/prefix-roles-courses.md), 118 тестов.
> Ниже — что осталось ДО СДАЧИ (живой прогон), по доработкам.

## Добор пакета №7/№8/№10 (PRPs/buttons-autorole-excel.md) — до сдачи

- [ ] №8: получить от Дмитрия ID отделов ↔ аббревиатуры (+должности) →
      заполнить roles.json departments → живой прогон: сотрудник замапленного
      отдела получает роль без меню, «(по отделу)» в приветствии
- [ ] №10: создать ПРИВАТНУЮ папку Диска для отчётов → REPORTS_FOLDER_ID в
      .env → «Отчёт» приносит xlsx; второй запуск заменяет файл (старый в
      корзине); проверить формат uploadfile по живому ответу
- [ ] №7: python scripts/register_commands.py --url {адрес сервера} →
      BUTTONS_ENABLED=1 + рестарт → нажать A-D/Готов/цифру роли; лог
      [command] покажет РЕАЛЬНЫЙ формат ONIMCOMMANDADD (поправить разбор при
      расхождении); рендер кнопок на мобильном; двойной клик = один ответ
- [x] №7: решить про кнопки HR-боту — РЕШЕНО «делаем», реализовано
      (PRPs/buttons-everywhere.md): кнопки HR-бота + все проактивные отправки
      + «Выбрать N», всё за тем же BUTTONS_ENABLED

## «Кнопки везде» (PRPs/buttons-everywhere.md) — до сдачи

- [ ] python scripts/register_commands.py --url {адрес} --bot hr (или both,
      если say ещё не регистрировали) → BUTTONS_ENABLED=1 + рестарт
- [ ] Живой лог [command] покажет ПОЛЕ имени команды в ONIMCOMMANDADD —
      при расхождении поправить двух кандидатов в /command
      (data[COMMAND][0][COMMAND] / data[PARAMS][COMMAND])
- [ ] Прокликать HR: Курсы → Вопросы N → Подтвердить N; Пригласить {email}
      из уведомления о новом сотруднике; Допустить {uid} из уведомления
      «завершил базовый тест»; Отмена в правке вопроса
- [ ] Прокликать employee: Начать обучение из анонса курса; Начать экзамен
      после «Допустить»; Выбрать N из «Мои курсы»; клавиатура в ежедневном
      напоминании соответствует состоянию
- [ ] Рендер NEWLINE-строк списка курсов на мобильном; 21+ pending-курсов →
      клавиатура капнута 20, ошибки KEYBOARD_OVERSIZE нет

## Качество вопросов и правка (PRPs/question-quality-and-editing.md) — до сдачи

- [ ] Серверный .env: OPENAI_MODEL=gpt-5.5 + рестарт контейнера
- [ ] scripts/dedup_courses.py на сервере: dry-run → --apply
      (ожидаемо в архив: 18, 21, 22, 23, 24, 25 — дубли гонки 29.07)
- [ ] «Перегенерировать {N}» руками для боевых курсов 13–20 (старые наборы
      полые: мета-вопросы, correct=A) — смотреть качество глазами на gpt-5.5
- [ ] Живой прогон: «Вопросы N» карточки (✅, листание, «все»), визард
      «Изменить N.q» (шаги, точка, цельный блок, Отмена), «Перегенерировать
      N.q» с кнопки «🔄 Заново»
- [ ] Hash-дедуп живьём: залить копию документа под другим именем → курс не
      создан, HR-уведомление «то же содержимое»

## Q&A-first (PRPs/qa-first-improvements.md) — до сдачи

- [ ] Живой eval-прогон: docker exec vertical-standards-bot python
      scripts/eval_qa.py --date YYYYMMDD → отчёт юзеру → Дмитрию
- [ ] ЮЗЕРУ: исходящий вебхук на «добавление файла на Диск» →
      http://195.63.168.145:8000/disk-webhook; после подключения поллер
      замедлить (POLL_INTERVAL_SEC=3600 в .env), не выключая — страховка
- [ ] После первых «Аббревиатура добавить» — перенести серверный
      data/roles.json в репо (иначе разъезжаются)
- [ ] Живо: пауза теста (⏸/выйти/Выбрать → возврат к тому же вопросу),
      источник-ссылка под RAG-ответом, «Мои документы» по ролям, анонс
      «стандарт доступен» при заливке нового файла, «Роль запретить» после
      появления маппинга отделов

## Дизайн клавиатур (PRPs/keyboard-design.md) — до сдачи

- [ ] Смоук цветов (5 минут, из спеки): одно сообщение с 4 BLOCK-кнопками
      #BEDC3C / #DDEFF8 / #004664 / #D6EFEC в тестовый чат → десктопная
      тема, тёмная тема, мобильное приложение. Фон не применяется —
      ничего не делать (иерархия на раскладке); применяется — проверить
      контраст лайма (белый на лайме запрещён)
- [ ] Мобильный рендер: ряды LINE с WIDTH не рвутся (роль 5×52, A–D 4×70,
      Выбрать 3×96, HR-пары 130+160); BLOCK-обрезки 38 (курсы) и 34
      (Пригласить/Допустить) не разворачиваются в 2 строки
- [ ] Прокликать 12 клавиатур по списку спеки: роль (≤8 имена / 17 сетка),
      чтение, тест (буквы и BLOCK-вариант с короткими телами), ожидание,
      «Мои курсы» (≤6 BLOCK / 7+ сетка), провал, проактивные ×3, HR-меню
      (лайм при непустой очереди), «Курсы» постранично (9+ курсов →
      «Показать ещё»), карточка вопроса (q=15 → «Подтвердить и запустить»
      тёмной плашкой), визард, уведомления (Допустить {Имя})

## №9 v2 «Дедлайны/напоминания/эскалация» (PRPs/deadlines-reminders-escalation.md) — до сдачи

- [ ] Живой прогон пересдачи: провалить экзамен → «Пересдать» → сдать →
      поздравление в Ленту (№5 не сломан)
- [ ] Напоминание себе: дождаться 9:00 МСК следующего дня (или REMINDER_HOUR_MSK=0
      временно) → «📅 Твои курсы и сроки» от employee-бота
- [ ] Эскалация: ESCALATION_DAYS=0 временно → сводка на u{uid Никиты}
      (n.sharapov резолвится user.get) РОВНО один раз; ступень 2 — там же
- [ ] Первый проактивный контакт с руководителем (CLIENT_ID — как автостарт №2)
- [ ] HR-команды «Руководители/добавить/удалить [старший]» живьём
- [ ] Проговорить клиенту: эскалации в ЧАТ Bitrix (протокол просит email —
      нужны SMTP-креды); заменить сид n.sharapov на реальных руководителей
- [ ] При деплое: baseline ставится автоматически первым стартом — сроки
      существующих курсов тикают С ДАТЫ ДЕПЛОЯ (клиенту так и объяснить)

## Протокол 05.08, пакет 1 (PRPs/ux-abbrevs-polish.md) — осталось до сдачи

- [ ] ЖИВОЙ рендер BB-кода: сообщение бота в чате показывает **жирный**, а не
      литеральный [b]…[/b]; если Bitrix не рендерит — откат md_to_bb на
      strip-звёздочек (одна строка: r"\1" вместо r"[b]\1[/b]")
- [ ] «Вопросы N» живьём: «→ B. Звонить 112» (текст варианта виден HR)
- [ ] «Выбрать N» с телефона: переключение курса, старый не «пройден»
- [ ] Секции отчёта по отделам с реальными данными
- [ ] Файл «F&B, CAT ....docx» в папке → роли fnb+catering (лог поллера)
- [ ] Сказать Дмитрию: SAL и S&M оба работают; ADM — отдельная роль
      «Администрация»; меню ролей теперь 17 пунктов (сократится в №8)

## Демо-фидбек 05.08 (PRPs/demo-feedback-pack.md) — осталось до сдачи (сервер)

- [ ] Живьём: «Подтвердить» тестового курса → уведомление реальному сотруднику
      от employee-бота («бот пишет первым», BOT_CLIENT_ID на сервере)
- [ ] «Мои курсы» с телефона (✅/▶️/⏳, подсказка команд в приветствии)
- [ ] Показать Дмитрию вопросы нового 2-этапного генератора (обещаны логи по
      «Негарантированным бронированиям»; локальный прогон зелёный — вопросы
      привязаны к документу; картинки из docx НЕ читаются — честно проговорить)
- [ ] ._*/.DS_Store из «Регламент_тест2» уехали в корзину Диска (лог поллера
      «junk → корзина»; Дмитрий в курсе, что мак их пересоздаст)
- [ ] «Отчёт»/«История»: ФИО (должность) · роль — должность появится у
      приглашённых ПОСЛЕ этого релиза (user.get при «Пригласить»); у старых
      записей должности нет — при желании разовый бэкфилл по user.get

## Доработка №11 «Префиксы департаментов» — осталось до сдачи (сервер)

- [ ] Серверный .env: `HR_USER_IDS=28528,36523` (36523 — тестировщик обоих ботов;
      сид employees подхватит на старте). Локальный .env уже ок
- [ ] На сервере: `python scripts/backfill_prefix_roles.py --dry-run` → боевой
      прогон → рестарт uvicorn (перетег уже заингещенных доков «Регламент_тест2»,
      чистка ._*-мусора из индекса, если попал)
- [ ] Вручную в портале: сделать 36523 администратором Битрикса (просьба Дмитрия)
- [ ] Живой прогон цепочки: сотрудник роли «Отдел бронирования» получает RES-курс,
      RAG-ответы фильтруются по роли; `._*.docx` в папке игнорируются (лог поллера)
- [ ] Вопросы Дмитрию: очерёдность курсов (ALL первыми или порядок активации?);
      что делать при неизвестном префиксе (сейчас — предупреждение HR);
      чистка корня 4698010 от нестандартов (договор Aiso, ТЗ и пр. индексируются
      как all_staff) / перенос MONITOR_FOLDER_ID на «Регламент_тест2» (4760370)

## Доработка №5 «Геймификация» — осталось до сдачи (живой прогон)

- [ ] Скоуп вебхука включает `log`; пройти экзамен тестово → пост «Благодарность»
      в Ленте (проверить, от чьего имени и виден ли всем; при неприемлемости —
      вебхук от служебного юзера)
- [ ] Рейтинг: дождаться понедельника 10:00 МСК или дёрнуть
      `gamification.maybe_post_weekly_rating()` вручную на сервере

## Доработка №2 «Доступ по email и автостарт» — осталось до сдачи (живой прогон)

- [ ] «Пригласить {реальный email}» через ngrok/сервер: бот пишет сотруднику первым
      (юзер, ни разу не открывавший диалог с ботом)
- [ ] Проверить формат фильтра `user.get`: «Пригласить несуществующий@email» → «не найден»
      (если найден кто-то — фильтр проигнорирован, переключить на `{"FILTER": {...}}`)
- [ ] Проверить скоуп вебхука: включает `user` (user_brief режет email)
- [ ] Проверить, есть ли OnUserAdd в списке «Исходящий вебхук» портала; если да —
      подписать URL `/user-webhook` (имя события придёт UPPERCASE)
- [ ] Заполнить `WATCH_DEPARTMENT_IDS` реальными ID отделов; первый прогон тихий
- [ ] Убедиться, что HR_USER_IDS на проде заполнен (гейт /hr)

## Доработка №1 «Поиск по ролям» — осталось до сдачи

- [ ] Заполнить реальные folder_id ролевых папок в `data/roles.json` (создать папки в Bitrix Disk)
- [ ] Живой прогон на сервере/ngrok: выбор роли в чате, вопрос от разных ролей
- [ ] Получить от Дмитрия вопросы с источником и ролью → пополнить `data/eval_roles.json`, гонять `scripts/eval_roles.py` на ежедневных созвонах

## Доработка №4 «Расширение загрузки» — осталось до сдачи (живой прогон)

- [ ] Подпапка в ролевой папке + документ → чанки с ролью корня (лог поллера;
      проверить имя поля UPDATE_TIME по разовому логу `file object keys`)
- [ ] Заливка .pptx/.txt/.pdf → курсы и чанки
- [ ] Замена: удалить+залить одноимённый docx → лог `replacing: -N`, вопросы курса прежние
- [ ] Замена через веб-UI (новая версия, тот же file_id) → реингест по UPDATE_TIME
- [ ] Удаление файла → чанки исчезли на ВТОРОМ полле, HR получил 🗑, курс архивный
- [ ] Файл документа в чат при старте курса (im.disk.file.commit: UPLOAD_ID vs DISK_ID;
      при фейле — только ссылка + лог [course-file])

## HR-команды

- [ ] Живьём: «Отчёт»/«История» с реальными данными — доходит ли длинное сообщение
      целиком (лимит Bitrix неизвестен; сейчас усечение 30 строк / 3 сессии)

## Курсы и назначение

- [x] ~~Назначение курса конкретному сотруднику или группе (сейчас все получают `active_courses[0]`)~~
      — закрыто №11: курс = первый непройденный с пересечением ролей
      (префиксы doc_name) × {роль сотрудника, all_staff}
- [ ] Команда HR `Назначить {course_id} {user_id}` — назначить курс вручную

## RAG-архитектура

- [ ] Поле `source_name` и `source_url` в чанке для ссылок в ответах
- [ ] Реранкер для узких запросов (известная хрупкость: «дежурная смена СПиР при пожаре» не входит в топ-16 при куче all_staff-чанков о пожаре)
- [ ] Legacy 842 чанка без doc_name/folder_id — замена/удаление их не касается;
      при желании — полное перестроение исходного корпуса через пайплайн

## Уведомления

- [ ] Разобраться поддерживает ли Bitrix `OnDiskFileAdd` через простой вебхук или только через OAuth-приложение (event.bind)

## Отложено

- [ ] Переход с OpenAI на Claude (модель для генерации и RAG)
- [ ] Масштабирование на франшизы / другие отели
- [ ] `/disk-webhook` сравнивает event без .upper() — события приходят UPPERCASE,
      живьём не сработал бы; чинить в №6 при подключении событий

## Готово

- [x] «Благодарность» в Ленту после сдачи экзамена (log.blogpost.add, фейл не
      роняет экзамен) + еженедельный рейтинг топ-5 сдавших (пн 10:00 МСК,
      идемпотентно через meta) (доработка №5)
- [x] Форматы PPTX/TXT/PDF + рекурсия подпапок до глубины 5 (доработка №4)
- [x] Per-document tracking: doc_name+folder_id в чанках пайплайна; замена версии
      (новый file_id ИЛИ UPDATE_TIME) без дублей; удаление чанков при удалении файла
      (two-strike) + архив курса (доработка №4)
- [x] app/index_store.py: мутации индекса под локом, атомарная запись — закрыта
      гонка параллельных ингестов (доработка №4)
- [x] Файл документа в чат при старте курса, с фолбэком на ссылку (доработка №4)
- [x] «Изменить {курс} {вопрос 1–15}» — правка вопроса целиком (текст+варианты+ответ+
      пояснение) двухшаговым флоу; покрывает и бэклог-пункты «Изменить вопрос»/«Ответ»
      (доработка №3)
- [x] «История {email|ID}» — вопросы и ответы сотрудника с ✓/✗ (доработка №3)
- [x] «Отчёт» — сводка по всем сессиям: этап, баллы, сдан/не сдан по 70% (доработка №3)
- [x] Whitelist сотрудников: таблица `employees`, сидинг legacy из sessions,
      отказ чужакам (доработка №2)
- [x] «Пригласить {email}»: user.get по EMAIL → whitelist → автостарт
      (бот пишет первым, сессия ROLE_SELECT) (доработка №2)
- [x] «Допустить» принимает email и ID; гейт /hr по HR_USER_IDS (доработка №2)
- [x] Поллер отделов (WATCH_DEPARTMENT_IDS, тихий первый прогон) +
      endpoint `/user-webhook` под OnUserAdd (доработка №2)

- [x] RAG-баг «пожар»: метадата `audience` (staff/guest) в чанках, guest никогда не отдаётся сотрудникам (доработка №1)
- [x] Системный промпт RAG: бот отвечает сотрудникам, не гостям
- [x] Роли в чанках: маппинг «папка → роль» (`data/roles.json`), бэкфилл 842 чанков, фильтр в `rag.py`
- [x] Выбор роли сотрудником (ROLE_SELECT) + команда «Роль» + `sessions.role`
- [x] Дедуп курсов по doc_name + таблица `processed_files` (копии документа в нескольких папках)
- [x] Тесты (pytest, 22) + ruff + eval-харнес `scripts/eval_roles.py` (6/6 живьём)
- [x] Поллинг Bitrix Disk каждые 5 минут
- [x] Парсинг и чанкинг `.docx`, `.md`
- [x] Генерация 5+10 вопросов через GPT с валидацией
- [x] Сохранение черновика в SQLite + JSON
- [x] HR-бот: Курсы / Вопросы / Подтвердить / Допустить
- [x] Стейт-машина сотрудника: READING → BASIC_TEST → WAITING_HR → EXAM → DONE
- [x] RAG Q&A в фазе READING
- [x] Уведомление HR после базового теста (с командой Допустить)
- [x] Уведомление сотрудника после допуска HR
- [x] Уведомление HR после завершения экзамена с результатами
- [x] disk-webhook эндпоинт (`/disk-webhook`) для OnDiskFileAdd
