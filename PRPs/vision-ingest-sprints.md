# Vision-ингест — журнал спринтов (протокол PRPs/vision-ingest.md, раздел D)

## Sprint 1 — 21.08 (базовая реализация)
Результат живого eval (10 медиа-объектов × 4 вопроса): **62% correct, 15 wrong → порог НЕ пройден.**

Диагноз по отчёту data/eval_vision_20260821.md:
- **Категория А (~9 wrong): описания неполны** — бот отвечает «не указано»,
  хотя деталь видна в оригинале (стрелка 1 → «Код гарантии», столбец после
  MONEY BALANCE, «ПС» → нижние строки, «Guest Recognition» справа от
  Billing Instr., пункт меню «Отменить бронь»). Причины: (1) vision-вызов
  БЕЗ `detail: "high"` — мелкий текст таблиц не читался; (2) промпт не
  требовал ПОЛНОЙ транскрипции структуры (колонки слева направо, пункты
  меню, значения).
- **Категория Б (~5 wrong): галлюцинации в ответах** — RAG дописывает
  выдуманные детали («заезд к 17:00») к верной основе. Причина: описания без
  дословных строк → LLM достраивает. Точная транскрипция строк должна
  закрыть большинство.
- Попутный дефект: вектор эмбеддинга строится по text[:2000] — хвост
  длинного описания неискаем; смягчение — структурные секции в НАЧАЛЕ
  описания.

## Sprint 2 — план (реализуется сейчас)
1. DESCRIBE_PROMPT v2 — обязательные секции в фиксированном порядке:
   (1) экран/система; (2) ПОЛНАЯ транскрипция структуры: заголовки колонок
   слева направо / пункты меню сверху вниз / подписи полей со значениями,
   примечательные строки таблиц дословно; (3) КАЖДАЯ аннотация: номер/цвет/
   форма → на какой точный элемент указывает (колонка + значение);
   (4) порядок действий нумерованными шагами. «Неразборчиво» вместо
   пропуска; ничего не выдумывать.
2. `detail: "high"` в image_url describe_image (в eval-судье оставляем auto —
   судья и так справлялся, его вердикты подтверждались вручную).
3. max_completion_tokens описания 2000 → 3000 (полная транскрипция длиннее).
4. Сброс vision_cache.json на сервере (промпт изменился — правило PRP).
5. Повторный живой eval тем же протоколом.

Гипотеза: категория А закрывается (1)+(2), категория Б — дословными
строками из (1). Цель ≥80% и 0 wrong.

Результат: **57% correct, 16 wrong — ХУЖЕ.** Диагноз оказался неполным.

## Sprint 3 — корень найден (последний по капу протокола)
Живая проверка кэша: **8 описаний из 10 медиа** — image1 (главная таблица
со стрелками 1/2/ПС, 790КБ) и ещё один скрин В ИНДЕКС НЕ ПОПАЛИ ВООБЩЕ.
Причина: gpt-5.5 — reasoning-модель; на сложных картинках невидимые
reasoning-токены съедают весь max_completion_tokens (3000) → content
приходит ПУСТЫМ; docx_media_chunks молча пропускал элемент («частичный
результат»), а судья спрашивал по оригиналу → гарантированные wrong.
Категории А/Б Sprint 2 были СЛЕДСТВИЕМ дыры, не промпта.

План:
1. _llm_text: пустой content → RuntimeError с finish_reason (не молчать).
2. describe_image: бюджет 8000, при пустом — повтор с 16000; после двух
   пустых — исключение наверх (лог «image N failed» уже есть).
3. Кэш НЕ сбрасывать: промпт не менялся, 8 валидных описаний
   переиспользуются; опишутся только недостающие.
4. Повторный живой eval.

